import { VisibilityTrackerDirective } from './visibility-tracker.directive';

describe('VisibilityTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
